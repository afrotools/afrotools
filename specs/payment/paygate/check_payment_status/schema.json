{
  "spec_version": "1.0",
  "provider_api_version": "v1",
  "capability": "check_payment_status",
  "capability_type": "synchronous",
  "status": "ready",
  "currency": [
    "XOF"
  ],
  "auth": {
    "type": "api_key",
    "location": "body",
    "field": "auth_token",
    "format": "{token}",
    "env_var": "PAYGATE_AUTH_TOKEN"
  },
  "endpoint": {
    "method": "POST",
    "url": "https://paygateglobal.com/api/v2/status"
  },
  "example_prompt": "Poll PayGate to know whether the customer validated the USSD push and the payment actually completed.",
  "input_schema": {
    "type": "object",
    "required": [
      "auth_token",
      "identifier"
    ],
    "properties": {
      "auth_token": {
        "type": "string",
        "description": "Merchant API key from the PayGate dashboard."
      },
      "identifier": {
        "type": "string",
        "description": "Your own unique reference originally passed to /v1/pay (initiate_payment). PayGate also exposes /api/v1/status which takes tx_reference instead — see gotchas."
      }
    }
  },
  "response_schema": {
    "type": "object",
    "properties": {
      "tx_reference": {
        "type": "string",
        "description": "Unique reference generated by PayGate for this transaction."
      },
      "identifier": {
        "type": "string",
        "description": "Your merchant identifier that was passed at initiation."
      },
      "payment_reference": {
        "type": "string",
        "description": "Operator-side reference (Flooz / T-Money). Empty until the payment is actually settled. Store this for support and reconciliation."
      },
      "status": {
        "type": "integer",
        "enum": [
          0,
          2,
          4,
          6
        ],
        "description": "Payment outcome. 0 = paid (success). 2 = pending (USSD push sent, customer has not validated yet). 4 = expired (customer did not validate in time). 6 = cancelled."
      },
      "datetime": {
        "type": "string",
        "description": "Date and time of the payment (when status = 0)."
      },
      "payment_method": {
        "type": "string",
        "enum": [
          "FLOOZ",
          "T-Money"
        ],
        "description": "Network used by the customer. Note the casing: 'T-Money' here is different from the 'TMONEY' value used on the network field of /v1/pay."
      }
    }
  },
  "error_schema": {
    "type": "object",
    "description": "Non-success responses use the same status codes (2/4/6) inside the body. HTTP-level errors are unusual; treat any non-JSON response as transient."
  },
  "gotchas": [
    "The status codes (0/2/4/6) on this endpoint mean PAYMENT outcomes — 0 = paid, 2 = pending, 4 = expired, 6 = cancelled. The IDENTICAL numbers on /v1/pay (initiate_payment) mean REGISTRATION outcomes (0=registered, 2=invalid token, 4=invalid params, 6=duplicate). Do not share a single status enum across both endpoints.",
    "status = 2 (pending) is the normal state immediately after initiate_payment — the customer has not yet entered their PIN. Re-poll every few seconds until you reach a terminal state (0, 4, or 6).",
    "payment_reference is the operator-side reference (Flooz or T-Money). It is what your customer-support agents need for reconciliation with the operator. Empty until status = 0.",
    "payment_method on the response uses 'T-Money' (with hyphen), while the `network` field you send to /v1/pay uses 'TMONEY' (uppercase, no hyphen). Do not assume they are the same string.",
    "PayGate also exposes /api/v1/status which takes `tx_reference` instead of `identifier`. /v2/status is usually more convenient because you already control the identifier from your side. Use whichever you persisted.",
    "Never fulfill an order based on the webhook alone — always re-check status via this endpoint server-side before releasing goods or funds.",
    "[MUST TELL USER] PayGate API documentation is private. Confirm response field shapes in your account dashboard if you see any mismatch in production."
  ]
}
